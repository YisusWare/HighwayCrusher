using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EnemyBrain : MonoBehaviour
{
    // Start is called before the first frame update
    [SerializeField]
    Aim360 aim;
    [SerializeField]
    DetectPlayer playerDetector;
    
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
       
    }
}
